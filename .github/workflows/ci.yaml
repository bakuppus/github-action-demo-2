name: Run One

on:
  pull_request:

jobs:
  one:
    runs-on: ubuntu-latest

    steps:
      - name: "Checkout Code"
        uses: "actions/checkout@v2"

      - name: workflow 1
        run: |
          echo "workflow 1 complete"


